name: gitHub information pipeline
on: workflow_dispatch
jobs:
  github-info-job:
    runs-on: ubuntu-latest
    steps:
      - name: Get Github Information Context
        run: echo "${{toJson(github)}}" # this command will print the github context information in json format